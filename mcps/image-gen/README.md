# mcp-image-gen (bundled)

Image-gen MCP server bundled with Todontic — the second v0.1 wedge (D-017).
Configurable backend (Replicate / OpenAI / Imagen / local ComfyUI). Powers the
Image-gen skill: prompt iteration → generate → branch variations → pick winner,
all inside the outline; writes attachments + a reproducibility sidecar JSON.

Stub — implementation lands with PRD-10 / PRD-15.
