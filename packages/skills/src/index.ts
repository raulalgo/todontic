/**
 * Skill format + loader (PRD-09 / PRD-12-14).
 *
 * Resolves a skill pack (folder with `skill.yml`, `system.md`, etc.) and its
 * declared MCP requirements at install time, handing MCP entries to
 * @todontic/mcp-installer. The skill packaging format is open and documented —
 * first-party skills (PM-refine, Designer, Code-handoff, Image-gen) use the SAME
 * format the marketplace exposes (Philosophy #8, D-013). Keep the format stable;
 * it's the most important spec in the project.
 *
 * Intentionally empty until PRD-09 lands.
 */

export {}
